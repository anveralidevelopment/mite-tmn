# Монитор активности клещей - Тюмень (Веб-версия)

Веб-сервис для мониторинга активности клещей в Тюменской области с красивым дизайном в стиле Bitrix24.

## Возможности

- 📊 **Веб-интерфейс** - современный дизайн в синих и зеленых тонах
- 📈 **Интерактивные графики** - динамика активности клещей
- 📰 **Источники данных** - парсинг с сайта и Telegram канала Роспотребнадзора
- 💾 **База данных PostgreSQL** - надежное хранение данных
- 🐳 **Docker** - легкий запуск в контейнерах
- 🔄 **Автообновление** - обновление данных через API

## Быстрый старт

### С Docker

1. **Клонируйте репозиторий и перейдите в директорию:**
```bash
cd mite-tmn
```

2. **Запустите приложение:**
```bash
cd docker
docker-compose up -d --build
```

Или из корня проекта:
```bash
docker-compose -f docker/docker-compose.yml up -d --build
```

3. **Откройте в браузере:**
```
http://localhost:5000
```

### Локально (без Docker)

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Настройте базу данных PostgreSQL** и создайте переменную окружения:
```bash
export DATABASE_URL=postgresql://user:password@localhost:5432/mite_tmn
```

3. **Запустите приложение:**
```bash
cd src
python app.py
```

Или с Gunicorn:
```bash
cd src
gunicorn --bind 0.0.0.0:5000 --workers 2 app:app
```

## Структура проекта

```
mite-tmn/
├── src/                # Исходный код приложения
│   ├── app.py          # Flask приложение
│   ├── parser.py       # Модуль парсинга данных
│   ├── database.py     # Работа с БД
│   └── logger_config.py # Настройка логирования
├── config/             # Конфигурация
│   └── config.json
├── docker/             # Docker конфигурация
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── docker-compose.prod.yml
├── templates/          # HTML шаблоны
│   └── index.html
├── static/             # Статические файлы
│   ├── style.css       # Стили в стиле Bitrix24
│   └── app.js          # JavaScript
├── requirements.txt    # Зависимости
└── README.md           # Документация
```

## API Endpoints

- `GET /` - Главная страница
- `GET /api/stats` - Получение статистики (текущая/прошлая неделя)
- `GET /api/sources?limit=20` - Список источников данных
- `GET /api/graph?start_date=YYYY-MM-DD&end_date=YYYY-MM-DD` - Данные для графика
- `POST /api/update` - Запуск обновления данных

## Дизайн

Интерфейс выполнен в стиле Bitrix24:
- **Синие тона**: `#2066B0` (основной), `#185a9a` (темный), `#2e7cd6` (светлый)
- **Зеленые акценты**: `#00c853` (основной), `#00e676` (светлый), `#00a844` (темный)
- Современные карточки и градиенты
- Адаптивный дизайн для мобильных устройств

## Конфигурация

Настройки находятся в `config.json`:
- Параметры парсинга (URL, таймауты, повторы)
- Уровни риска и их пороги
- Настройки логирования

## База данных

Приложение использует PostgreSQL для хранения данных. Таблицы создаются автоматически при первом запуске.

### Подключение к БД в Docker:

```bash
docker-compose exec db psql -U mite_user -d mite_tmn
```

## Остановка

```bash
cd docker
docker-compose down
```

Или из корня:
```bash
docker-compose -f docker/docker-compose.yml down
```

Для удаления данных БД:
```bash
docker-compose down -v
```

## Логи

Логи сохраняются в директории `logs/` и ротируются автоматически.

## Лицензия

Проект предназначен для личного использования.
