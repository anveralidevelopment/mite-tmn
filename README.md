# Монитор активности клещей - Тюмень

Программа для мониторинга активности клещей в Тюменской области. Парсит информацию с сайта и Telegram-канала Роспотребнадзора и выводит график активности.

## Возможности

- 📊 Мониторинг активности клещей в реальном времени
- 📈 Визуализация данных в виде графиков
- 📰 Парсинг данных из нескольких источников:
  - Веб-сайт Роспотребнадзора
  - RSS-лента
  - Telegram-канал
- 💾 Сохранение данных в Excel и JSON
- 🎨 Современный интерфейс с неоновой подсветкой
- 🔊 Звуковые эффекты
- 📊 Экспорт данных в Excel
- 🔄 Автоматическое обновление данных

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Убедитесь, что у вас есть звуковые файлы (опционально):
   - `forest.wav` - фоновый звук
   - `button.wav` - звук кнопки
   - `update.wav` - звук обновления

3. Запустите программу:
```bash
python mite.py
```

## Конфигурация

Программа использует файл `config.json` для настройки. Если файл отсутствует, используются значения по умолчанию.

### Основные параметры:

- **app**: Настройки приложения (название, размер окна, файлы данных)
- **ui**: Цветовая схема и звуки
- **parsing**: Настройки парсинга (URL, таймауты, количество повторов)
- **risk_levels**: Пороги и цвета для уровней риска
- **graph**: Настройки графика (количество недель для отображения)
- **logging**: Настройки логирования

Пример конфигурации находится в файле `config.json`.

## Логирование

Программа ведет логи в файл `app.log`. Логи ротируются при достижении 10 МБ (5 резервных копий).

## Структура проекта

```
mite-tmn/
├── mite.py              # Основной файл приложения
├── logger_config.py      # Модуль логирования
├── config.json           # Файл конфигурации
├── requirements.txt      # Зависимости Python
├── tick_data.json        # Данные в формате JSON
├── tick_stats.xlsx       # Данные в формате Excel
├── app.log              # Файл логов
└── README.md            # Этот файл
```

## Улучшения в новой версии

### ✅ Исправленные баги:
- Исправлен баг в `update_filtered_graph` (использовался `week_label` вместо `weeks`)

### ✨ Новые возможности:
- Добавлен модуль логирования с ротацией файлов
- Конфигурационный файл для гибкой настройки
- Retry логика для HTTP запросов
- Улучшенная обработка ошибок
- Все константы вынесены в конфигурацию

### 🔧 Улучшения:
- Лучшая структура кода
- Подробное логирование операций
- Настраиваемые пороги риска
- Настраиваемые таймауты и количество повторов

## Использование

1. **Обновление данных**: Нажмите кнопку "🔄 Обновить данные" для получения свежей информации
2. **Фильтрация**: Используйте панель фильтров для выбора периода
3. **Экспорт**: Нажмите "📁 Экспорт в Excel" для сохранения данных
4. **Звук**: Переключайте звук кнопкой "🔊 Звук Вкл/Выкл"
5. **Легенда**: Просмотрите легенду уровней риска кнопкой "📊 Легенда рисков"

## Уровни риска

- **Низкий**: менее 50 случаев
- **Умеренный**: 50-99 случаев
- **Высокий**: 100-149 случаев
- **Очень высокий**: 150+ случаев

Пороги можно настроить в `config.json`.

## Требования

- Python 3.8+
- Windows 10-11 (может работать на других ОС)
- Интернет-соединение для парсинга данных

## Лицензия

Этот проект предназначен для личного использования.

## Поддержка

При возникновении проблем проверьте файл `app.log` для диагностики.
